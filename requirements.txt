pyzk       # leer biométrico ZKTeco vía TCP (puerto 4370)
openpyxl   # escribir .xlsx
polars     # parquet (sync) + reporte_core
duckdb     # MotherDuck (md:...) y consultas locales
pyyaml     # raw/reglas/colaboradores.yaml
fastapi    # servicio web reports/biometrico
uvicorn[standard]
